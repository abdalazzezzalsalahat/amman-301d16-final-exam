<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>the simpsons</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header></header>

    <div class="con">
        <% data.forEach(info => { %>
            <div class="card">
                <h2>hello I am <% info.character %></h2>
                <img src="<% info.image %>" alt="<% info.character %>">
                <h3><% info.quote %></h3>
                <form action="/myFavorites" method="POST">
                    <input type="hidden" name="<% info.character %>">
                    <input type="submit" value="Add to favorites">
                </form>
            </div>
        <% }); %>
    </div>

    <footer></footer>
</body>
</html>